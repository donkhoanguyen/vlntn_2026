# Optional: your email so "Always Innovating" submissions open the visitor's
# email client with this as the To address. They hit Send and you get the responses.
# Create a file named .env in this folder and add (use your real email):
#
# VITE_INNOVATING_EMAIL=you@example.com
#
# If you don't set this, the email will open with an empty To field and the
# visitor can type your address before sending.
